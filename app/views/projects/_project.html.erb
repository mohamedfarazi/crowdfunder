<div class="display_projects">
<%= link_to (image_tag(project.image, size: "300x500")), project %>
<h2><%= project.name %></h2>
<h3>Goal: <%= number_to_currency(project.goal_in_cents.to_f/100) %></h3>
</div>